# 菜单界面重构 - 任务列表

## 任务概览

本任务列表按照实现顺序组织，每个任务都是独立可测试的单元。

---

## 1. 基础结构准备

- [x] 1.1 添加菜单相关全局变量
  - 在 `xuanniu.h` 中声明 `menu_selected`, `encoder_delta`, `auto_enter`
  - 在 `xuanniu.c` 中定义并初始化这些变量
  - **验证**：编译通过，无警告
  - **关联需求**：需求 1.1, 需求 5.1

- [x] 1.2 修改开机初始化逻辑
  - 在 `main.c` 中将 `ui` 初始化为 5（菜单界面）
  - 将 `chu` 初始化为 5（需要初始化菜单）
  - 将 `menu_selected` 初始化为 1（默认选中 UI1）
  - **验证**：开机后 ui=5, chu=5, menu_selected=1
  - **关联需求**：需求 1.1, 需求 4.1

- [x] 1.3 添加菜单界面位置常量
  - 在 `lcd.h` 中定义菜单项位置、尺寸、颜色常量
  - **验证**：编译通过
  - **关联需求**：需求 8.1

---

## 2. 编码器增量统一管理

- [x] 2.1 重构 Encoder() 函数开头
  - 在周期控制后立即调用 `Encoder_GetDelta()` 并保存到 `encoder_delta`
  - **验证**：`encoder_delta` 每 20ms 更新一次
  - **关联需求**：需求 5.1

- [x] 2.2 修改 UI1 的编码器逻辑
  - 将 `Encoder_GetDelta()` 调用替换为使用 `encoder_delta`
  - **验证**：UI1 旋转调速功能正常
  - **关联需求**：需求 5.2, 需求 9.1

- [x] 2.3 修改 UI2 的编码器逻辑
  - 在 `LCD()` 函数中将 `Encoder_GetDelta()` 替换为 `encoder_delta`
  - **验证**：UI2 预设选择功能正常
  - **关联需求**：需求 5.4, 需求 9.2

- [x] 2.4 修改 UI3 的编码器逻辑
  - 修改 `tiao_se()` 函数使用 `encoder_delta`
  - **验证**：UI3 RGB 调色功能正常
  - **关联需求**：需求 5.4, 需求 9.3

- [x] 2.5 修改 UI4 的编码器逻辑
  - 修改 `deng_ui4()` 函数使用 `encoder_delta`
  - **验证**：UI4 亮度调节功能正常
  - **关联需求**：需求 5.4, 需求 9.4

---

## 3. 菜单界面显示

- [x] 3.1 实现菜单界面初始化函数
  - 在 `lcd.c` 中添加 `LCD_Menu_Init()` 函数
  - 清屏并绘制背景
  - **验证**：调用后屏幕显示黑色背景
  - **关联需求**：需求 8.4

- [x] 3.2 实现菜单项绘制函数
  - 在 `lcd.c` 中添加 `Draw_Menu_Items()` 函数
  - 绘制 4 个菜单项文字（"速度"/"预设"/"调色"/"亮度"）
  - **验证**：4 个文字正确显示在预定位置
  - **关联需求**：需求 1.4, 需求 8.1

- [x] 3.3 实现选中边框绘制函数
  - 在 `lcd.c` 中添加 `Draw_Menu_Border(uint8_t item, uint8_t show)` 函数
  - show=1 绘制边框，show=0 清除边框
  - **验证**：边框可正确绘制和清除
  - **关联需求**：需求 1.5, 需求 8.2

- [x] 3.4 在 LCD() 中添加菜单界面处理分支
  - 添加 `if (ui == 5)` 分支
  - 实现 `chu == 5` 时的初始化逻辑
  - 实现选中项变化时的边框更新
  - **验证**：开机显示菜单界面，默认选中 UI1
  - **关联需求**：需求 1.1, 需求 4.5

---

## 4. 菜单界面交互

- [x] 4.1 实现菜单界面旋转切换
  - 在 `Encoder()` 中添加 `ui == 5` 时的旋转处理
  - 使用 `encoder_delta` 更新 `menu_selected`
  - 实现 1-4 循环边界处理
  - **验证**：旋转编码器可循环切换选中项
  - **关联需求**：需求 1.2, 需求 5.3

- [x] 4.2 实现菜单界面单击进入
  - 在 `Encoder()` 的单击处理中添加 `ui == 5` 分支
  - 单击时设置 `ui = menu_selected`, `chu = menu_selected`
  - **验证**：单击可进入选中的 UI
  - **关联需求**：需求 1.3, 需求 10.1

- [x] 4.3 实现菜单界面长按无操作
  - 在长按检测中，当 `ui == 5` 时不执行任何操作
  - **验证**：菜单界面长按无反应
  - **关联需求**：需求 2.4

---

## 5. 返回菜单功能

- [x] 5.1 修改双击检测逻辑实现返回菜单
  - 移除原有的双击开关雾化器功能
  - 添加双击返回菜单逻辑：`ui = 5`, `chu = 5`, `menu_selected = 原ui`
  - **验证**：UI1-4 双击返回菜单
  - **关联需求**：需求 2.1, 需求 2.2

- [x] 5.2 修改 UI4 的 chu 变量逻辑
  - 将 `chu = 1` 改为 `chu = 5`（返回菜单而非 UI1）
  - **验证**：UI4 初始化完成后 chu=5
  - **关联需求**：需求 4.3

- [x] 5.3 实现子状态重置
  - 在 UI2 初始化时重置 `pei_se_state = 0`
  - 在 UI3 初始化时重置 `tiao_se_num = 0`
  - 在 UI4 初始化时重置 `brt_state = 0`
  - **验证**：每次进入 UI 都是初始状态
  - **关联需求**：需求 7.1, 需求 7.2, 需求 7.3

---

## 6. 油门模式特殊处理

- [x] 6.1 油门模式下禁用双击返回
  - 在双击检测中添加 `wuhuaqi_state != 2` 条件
  - **验证**：油门模式下双击不返回菜单
  - **关联需求**：需求 3.2

- [x] 6.2 修改油门模式旋转退出目标
  - 将退出后的 `ui` 设为 5（菜单）而非 1
  - 设置 `chu = 5`, `menu_selected = 1`
  - **验证**：油门模式旋转退出到菜单
  - **关联需求**：需求 3.4

- [x] 6.3 修改油门模式三击退出目标
  - 将退出后的 `ui` 设为 5（菜单）而非 1
  - 设置 `chu = 5`, `menu_selected = 1`
  - **验证**：油门模式三击退出到菜单
  - **关联需求**：需求 3.5

---

## 7. 蓝牙命令集成

- [x] 7.1 修改 UI:X 命令处理
  - 修改 `rx.c` 中的 `UI:X` 命令处理逻辑
  - 设置 `ui = 5`, `chu = 5`, `menu_selected = X`, `auto_enter = 1`
  - **验证**：APP 发送 UI:2 后硬件切换到 UI2
  - **关联需求**：需求 6.1, 需求 6.2, 需求 6.3

- [x] 7.2 实现自动进入逻辑
  - 在 `LCD()` 的菜单处理中检测 `auto_enter`
  - 当 `chu == 0 && auto_enter` 时自动进入选中的 UI
  - **验证**：蓝牙切换界面后自动进入目标 UI
  - **关联需求**：需求 6.3

---

## 8. 测试与优化

- [ ] 8.1 功能完整性测试（硬件测试）
  - 测试开机进入菜单界面（ui=5, chu=5, menu_selected=1）
  - 测试旋转编码器切换选中项（1→2→3→4→1 循环）
  - 测试单击编码器进入各 UI（UI1/UI2/UI3/UI4）
  - 测试双击编码器返回菜单（记忆上次位置）
  - 测试油门模式特殊处理（长按加速、旋转/三击退出到菜单）
  - 测试蓝牙 UI:X 命令切换界面
  - **验证**：所有功能正常工作
  - **关联需求**：需求 1.1-1.5, 需求 2.1-2.4, 需求 3.1-3.5, 需求 6.1-6.4

- [ ] 8.2 兼容性测试（硬件测试）
  - 测试 UI1 风速调节（旋转编码器调速）和油门模式（三击进入）
  - 测试 UI2 预设配色选择（单击进入选择模式，旋转切换预设）
  - 测试 UI3 RGB 调色（单击切换灯带/RGB通道，旋转调节数值）
  - 测试 UI4 亮度调节（单击进入调节模式，旋转调节亮度）
  - 测试蓝牙通信功能（SPEED/PRESET/BRIGHT 等命令）
  - **验证**：原有功能不受影响
  - **关联需求**：需求 9.1-9.5

- [ ] 8.3 边界情况测试（硬件测试）
  - 测试 menu_selected 边界循环（1→4→1 和 4→1→4）
  - 测试快速旋转响应（不跳过选项）
  - 测试连续长按处理（不重复触发）
  - 测试子状态重置（每次进入 UI 都是初始状态）
  - **验证**：边界情况处理正确
  - **关联需求**：需求 7.1-7.5, 需求 10.1-10.5

- [ ]\* 8.4 性能优化（可选）
  - 优化菜单界面刷新策略（局部刷新）
  - 减少不必要的 LCD 重绘（边框高亮方式）
  - **验证**：界面切换流畅无闪烁
  - **关联需求**：需求 8.3-8.5

---

## 任务依赖关系

```
1.1 ─┬─> 2.1 ─┬─> 2.2
     │        ├─> 2.3
1.2 ─┤        ├─> 2.4
     │        └─> 2.5
1.3 ─┴─> 3.1 ─> 3.2 ─> 3.3 ─> 3.4 ─> 4.1 ─> 4.2 ─> 4.3
                                      │
                                      v
                              5.1 ─> 5.2 ─> 5.3 ─> 6.1 ─> 6.2 ─> 6.3
                                                          │
                                                          v
                                                  7.1 ─> 7.2 ─> 8.1 ─> 8.2 ─> 8.3
```

---

## 预计工时

| 阶段 | 任务数 | 状态 |
|------|--------|------|
| 基础结构准备 | 3 | ✅ 已完成 |
| 编码器增量统一管理 | 5 | ✅ 已完成 |
| 菜单界面显示 | 4 | ✅ 已完成 |
| 菜单界面交互 | 3 | ✅ 已完成 |
| 返回菜单功能 | 3 | ✅ 已完成 |
| 油门模式特殊处理 | 3 | ✅ 已完成 |
| 蓝牙命令集成 | 2 | ✅ 已完成 |
| 测试与优化 | 4 | ⏳ 待测试 |
| **总计** | **27** | **23/27 完成** |

## 实现状态总结

所有代码实现任务（1.1-7.2）已完成，剩余任务为硬件测试任务（8.1-8.4）。

### 已实现功能：
- ✅ 菜单界面（ui=5）：开机显示，4个菜单项（速度/预设/调色/亮度）
- ✅ 编码器旋转切换选中项（1-4循环）
- ✅ 单击进入选中的UI
- ✅ 双击返回菜单（记忆位置）- 原长按返回已改为双击返回
- ✅ 油门模式特殊处理（长按加速，旋转/三击退出到菜单）
- ✅ 蓝牙 UI:X 命令通过菜单切换界面
- ✅ 子状态重置（pei_se_state, tiao_se_num, brt_state）
- ✅ 编码器增量统一管理（encoder_delta 全局变量）
- ✅ 移除双击开关雾化器功能（由其他方式替代）
